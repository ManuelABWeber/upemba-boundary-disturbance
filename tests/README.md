# Tests

Reference scalar and classification checks plus downstream publication validation utilities live here. The tests consume the retained authoritative analytical tables in this repository and do not require an ignored local path configuration or access to the legacy `Chapter_1` working repository.
